{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    // The base config pulls in Jest's globals; this app has no Jest.
    "types": []
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
